
## Task: Create User entity

**Timestamp:**
2026-03-27T12:38:00Z

**What was done:**
Created the User JPA entity with id (auto-generated Long PK), name (required, unique), and email (required, unique) fields. Added Spring Data JPA, Spring Web, H2, and Spring Boot Test dependencies to pom.xml since the project had no dependencies. The entity uses the "users" table name to avoid the reserved word "user" in H2.

**What was changed:**
- pom.xml (added spring-boot-starter-data-jpa, spring-boot-starter-web, h2, spring-boot-starter-test)
- src/main/java/com/programacho/ralphloopexample/User.java (new)
- src/test/java/com/programacho/ralphloopexample/UserTests.java (new)

**Remarks:**
Added spring-boot-starter-web and spring-boot-starter-test proactively since they are required by the tech stack and upcoming tasks.

## Task: Create UserRepository

**Timestamp:**
2026-03-27T12:40:00Z

**What was done:**
Created UserRepository interface extending JpaRepository<User, Long> with custom finder methods findByName and findByEmail. Added 7 unit tests covering save, findAll, findById, findByName, findByEmail, and deleteById operations including not-found cases.

**What was changed:**
- src/main/java/com/programacho/ralphloopexample/UserRepository.java (new)
- src/test/java/com/programacho/ralphloopexample/UserRepositoryTests.java (new)
- pom.xml (added spring-boot-starter-data-jpa-test for @DataJpaTest support)
- prd.md (marked task complete)

**Remarks:**
In Spring Boot 4, @DataJpaTest moved from org.springframework.boot.test.autoconfigure.orm.jpa to org.springframework.boot.data.jpa.test.autoconfigure, and requires the separate spring-boot-starter-data-jpa-test dependency.

## Task: Create UserService with CRUD methods

**Timestamp:**
2026-03-27T12:42:00Z

**What was done:**
Created UserService with CRUD methods: create, findAll, findById, update, and delete. The update method finds the existing user by ID, applies changes, and saves. The delete method returns a boolean indicating whether the user existed. Added 8 unit tests using Mockito to mock UserRepository, covering all methods including not-found cases.

**What was changed:**
- src/main/java/com/programacho/ralphloopexample/UserService.java (new)
- src/test/java/com/programacho/ralphloopexample/UserServiceTests.java (new)
- prd.md (marked task complete)

**Remarks:**
No new dependencies needed. Used @ExtendWith(MockitoExtension.class) with @Mock and @InjectMocks for lightweight unit tests that don't load the Spring context.

## Task: Create UserController with CRUD endpoints

**Timestamp:**
2026-03-27T12:45:00Z

**What was done:**
Created UserController as a @RestController with @RequestMapping("/users") implementing all five CRUD endpoints: POST /users (201 Created), GET /users (200), GET /users/{id} (200/404), PUT /users/{id} (200/404), and DELETE /users/{id} (204/404). Added 8 unit tests using @WebMvcTest with MockMvc and a mocked UserService covering all endpoints including not-found cases.

**What was changed:**
- src/main/java/com/programacho/ralphloopexample/UserController.java (new)
- src/test/java/com/programacho/ralphloopexample/UserControllerTests.java (new)
- pom.xml (added spring-boot-starter-webmvc-test for @WebMvcTest support)
- prd.md (marked task complete)

**Remarks:**
In Spring Boot 4, @WebMvcTest moved from org.springframework.boot.test.autoconfigure.web.servlet to org.springframework.boot.webmvc.test.autoconfigure, and requires the separate spring-boot-starter-webmvc-test dependency. Jackson's ObjectMapper is not on the test classpath with just the webmvc-test starter, so used inline JSON text blocks instead.

## Task: Add exception handling for 404 when the user is not found

**Timestamp:**
2026-03-27T12:48:00Z

**What was done:**
Introduced exception-based 404 handling using a custom UserNotFoundException and a @RestControllerAdvice GlobalExceptionHandler. Refactored UserService to throw UserNotFoundException instead of returning Optional/boolean for findById, update, and delete. Simplified UserController by removing inline Optional handling. The 404 response now includes a JSON body with an error message (e.g., {"error": "User not found with id: 99"}). Added unit tests for GlobalExceptionHandler and updated existing UserService and UserController tests.

**What was changed:**
- src/main/java/com/programacho/ralphloopexample/UserNotFoundException.java (new)
- src/main/java/com/programacho/ralphloopexample/GlobalExceptionHandler.java (new)
- src/main/java/com/programacho/ralphloopexample/UserService.java (modified)
- src/main/java/com/programacho/ralphloopexample/UserController.java (modified)
- src/test/java/com/programacho/ralphloopexample/GlobalExceptionHandlerTests.java (new)
- src/test/java/com/programacho/ralphloopexample/UserServiceTests.java (modified)
- src/test/java/com/programacho/ralphloopexample/UserControllerTests.java (modified)
- prd.md (marked task complete)

**Remarks:**
Chose to refactor UserService return types from Optional<User>/boolean to User/void, throwing UserNotFoundException on not-found. This simplifies both the service API and the controller, pushing error handling to the centralized @RestControllerAdvice. The not-found tests now verify both the 404 status and the error message in the response body.

## Task: Add exception handling for 409 when the name or email is already taken

**Timestamp:**
2026-03-27T12:50:00Z

**What was done:**
Created UserAlreadyExistsException for 409 Conflict responses. Added duplicate name/email checks in UserService.create() and UserService.update() methods. The update method correctly allows a user to keep their own name/email by filtering out the current user when checking for duplicates. Added a handler in GlobalExceptionHandler to return 409 with a JSON error message. Added 6 new tests: 2 for create conflicts (name and email), 2 for update conflicts (name and email), 1 controller test for create conflict, 1 controller test for update conflict, and 1 exception handler unit test.

**What was changed:**
- src/main/java/com/programacho/ralphloopexample/UserAlreadyExistsException.java (new)
- src/main/java/com/programacho/ralphloopexample/GlobalExceptionHandler.java (modified)
- src/main/java/com/programacho/ralphloopexample/UserService.java (modified)
- src/test/java/com/programacho/ralphloopexample/UserServiceTests.java (modified)
- src/test/java/com/programacho/ralphloopexample/UserControllerTests.java (modified)
- src/test/java/com/programacho/ralphloopexample/GlobalExceptionHandlerTests.java (modified)
- prd.md (marked task complete)

**Remarks:**
The duplicate check in update() uses `.filter(existing -> !existing.getId().equals(id))` to allow a user to keep their own name or email unchanged during an update, only rejecting when another user holds the same value.

## Task: Add Spring Actuator and expose only the health endpoint

**Timestamp:**
2026-03-27T12:52:00Z

**What was done:**
Added the spring-boot-starter-actuator dependency to pom.xml. Configured application.yaml to expose only the health endpoint via `management.endpoints.web.exposure.include: health`. Added 2 unit tests: one verifying the /actuator/health endpoint returns status UP, and one verifying that other actuator endpoints (env, beans) are not exposed (return 404).

**What was changed:**
- pom.xml (added spring-boot-starter-actuator dependency)
- src/main/resources/application.yaml (added management.endpoints.web.exposure.include: health)
- src/test/java/com/programacho/ralphloopexample/ActuatorHealthTests.java (new)
- prd.md (marked task complete)

**Remarks:**
In Spring Boot 4, @AutoConfigureMockMvc moved from org.springframework.boot.test.autoconfigure.web.servlet to org.springframework.boot.webmvc.test.autoconfigure. Used @SpringBootTest with @AutoConfigureMockMvc instead of @WebMvcTest to ensure the full actuator infrastructure is loaded.
